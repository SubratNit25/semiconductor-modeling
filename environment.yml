name: semiconductor-modeling

channels:
  - conda-forge

dependencies:

  # ============================================================
  # Python
  # ============================================================
  - python=3.11

  # ============================================================
  # Jupyter
  # ============================================================
  - jupyterlab
  - ipykernel
  - nbconvert
  - ipympl
  - xeus-cpp

  # ============================================================
  # Scientific Computing
  # ============================================================
  - numpy
  - scipy
  - pandas
  - matplotlib
  - sympy
  - h5py
  - pyarrow
  - zarr

  # ============================================================
  # Machine Learning / Numerical Acceleration
  # ============================================================
  - scikit-learn
  - numba
  - jax

  # ============================================================
  # C / C++ Development
  # ============================================================
  - c-compiler
  - cxx-compiler
  - clang-tools
  - cmake
  - ninja
  - make
  - gdb

  # ============================================================
  # Python <-> C/C++
  # ============================================================
  - pybind11
  - cython
  # ============================================================
  # CUDA / NVIDIA GPU DEVELOPMENT
  # ============================================================
  - cuda-nvcc
  - cuda-nvrtc-dev
  - cuda-cudart-dev
  - cuda-driver-dev
  # ============================================================
  # Parallel Computing
  # ============================================================
  - openmpi
  - mpi4py
  - dask
  - distributed

  # ============================================================
  # Optimization / Parameter Fitting
  # ============================================================
  - lmfit

  # ============================================================
  # Testing
  # ============================================================
  - pytest

  # ============================================================
  # Code Quality
  # ============================================================
  - black
  - ruff
  - pre-commit

  # ============================================================
  # Documentation
  # ============================================================
  - sphinx
  - myst-parser

  # ============================================================
  # Utilities
  # ============================================================
  - tqdm
  - pyyaml
  - psutil
  - pip
